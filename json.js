// json is a text format that api returns when making request
// looks like an array with javascript object inside it but all is just a string format
// when browser exchanging data to the server it has to be done in text format
// to use the json we need to turn it into real javascript objects so that we can access all